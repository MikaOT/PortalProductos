# Portal de productos con JWT + Roles + Chat (Socket.IO)


## Requisitos
- Node.js 18+
- MongoDB en local o remoto


## Configuración
1. Clona el repositorio (o copia estos archivos).
2. Crea `.env` desde `.env.example` y ajusta `MONGODB_URI`, `JWT_SECRET` y `CLIENT_ORIGIN`.


## Instalación y ejecución
```bash
npm install
npm run dev # desarrollo con nodemon
# o
npm start # producción